# Changelog

## 0.1.2

- 兼容 B 站市集请求头，提升商品查询链路的兼容性。
- 修正 Caddy `/api/*` 路由优先级，避免 API 请求被 SPA fallback 返回为 `index.html`。
- 增强注册校验、确认密码、字段错误提示及用户可见中文错误映射；并发重复用户名统一返回中文 409。
- 修复商品查询的 ID 规范化、错误态反馈及响应式布局，避免宽屏或窄屏按钮异常换行。
- 增加 Docker 构建上下文排除规则，避免将本地依赖和构建产物带入镜像上下文。

## 0.1.1

- 修复前端 Docker 镜像构建时 Caddyfile 源路径错误：按根目录构建上下文使用 `frontend/Caddyfile`。

## 0.1.0

- 初始版本发布。

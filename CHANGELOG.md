# Changelog

## 0.2.0

- 新增长期价格历史分层存储：最近 24 小时保留原始成功检查点，较旧数据依次聚合为 1 分钟、5 分钟和 30 分钟桶，并保留区间最低价、最高价、末值、状态和样本数。
- 新增独立于邮件 Outbox 的站内通知中心，覆盖低价触发、邮箱绑定/换绑、管理员定向通知与广播，并提供未读计数、已读状态及自动清理。
- 增强管理员后台的分页用户管理、监控负载估算、历史存储统计、通知运营和安全凭据管理；同时完成可配置 API Base URL 与 CDN 缓存部署准备。

## 0.1.5

- 每次成功取得 B 站最新商品状态时写入一个价格观察点；缓存命中和失败回退不制造历史，并为长期历史增加范围查询、返回点数限制及保留首尾和关键波动的流式降采样。
- 收藏卡片按后端 `next_check_at` 显示自动检查倒计时，页面可见时低频同步 Worker 状态；价格历史升级为支持真实时间轴、范围切换、桌面悬停和移动端触摸详情的交互图表。
- 收束邮箱验证状态：成功后立即停止重发倒计时并锁定邮箱，新增可访问的成功弹窗，以及保持旧邮箱有效直至新邮箱验证完成的“更换邮箱”流程。

## 0.1.4

- 补充真实 163 SMTP 的安全部署与验收步骤：使用未跟踪的本机 `.env` 注入客户端授权码，明确 465/SSL 配置、容器重建、健康检查、日志和实际收件验证方法。
- 统一后端、前端、配置模板和项目元数据版本号，避免部署健康信息与发布版本不一致。

## 0.1.3

- 按 B 站市集真实响应补充商品标题和封面解析；增加受限 B 站 CDN 封面代理与前端失败占位，解决浏览器防盗链 403。
- 增加 163 邮箱 SSL 配置指引，明确邮箱验证、SMTP 授权码和发送失败处理方式；发送失败时不会生成有效验证码。
- 补充 BiliMarketBoss 的 ClsId 六步检索教程、应用内 `/tutorial` 入口及配套教程素材说明。

## 0.1.2

- 兼容 B 站市集请求头，提升商品查询链路的兼容性。
- 修正 Caddy `/api/*` 路由优先级，避免 API 请求被 SPA fallback 返回为 `index.html`。
- 增强注册校验、确认密码、字段错误提示及用户可见中文错误映射；并发重复用户名统一返回中文 409。
- 修复商品查询的 ID 规范化、错误态反馈及响应式布局，避免宽屏或窄屏按钮异常换行。
- 增加 Docker 构建上下文排除规则，避免将本地依赖和构建产物带入镜像上下文。

## 0.1.1

- 修复前端 Docker 镜像构建时 Caddyfile 源路径错误：按根目录构建上下文使用 `frontend/Caddyfile`。

## 0.1.0

- 初始版本发布。
